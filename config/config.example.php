<?php
declare(strict_types=1);

// Diese Datei nach config/config.php kopieren und den echten Key eintragen.
// config/config.php wird per .gitignore NICHT ins Repo übernommen.

define('VT_API_KEY', 'DEIN_VIRUSTOTAL_API_KEY');
